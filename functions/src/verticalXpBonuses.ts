/** One-time per user on first approved leap (mirrored in `src/lib/verticalScore.ts` if constants are duplicated). */
export const FIRST_LEAP_BONUS_XP = 25;

/** Once globally per `challengeDate` / day key on first approval that day. */
export const FIRST_POST_OF_DAY_BONUS_XP = 15;

export const DAILY_CHALLENGE_STATS_COLLECTION = 'dailyChallengeStats';
