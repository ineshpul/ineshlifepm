
  # Finalize Product Requirements Document

  This is a code bundle for Finalize Product Requirements Document. The original project is available at https://www.figma.com/design/QNSZdz4vpwK2fmdjW10cRz/Finalize-Product-Requirements-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  