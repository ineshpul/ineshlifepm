/** Shown on Today / Record after the leap drops (noon Eastern); not editable by admin. */
export const CHALLENGE_INSTRUCTIONS = 'Post it fast';

/** Midnight–noon Eastern, before the day’s leap is revealed to players. */
export const CHALLENGE_PRE_DROP_TITLE = "Today's leap is loading...";

/** Replaces “Post it fast · Up to Ns” before noon Eastern. */
export const CHALLENGE_PRE_DROP_INSTRUCTIONS = 'Be first · Be fast';
